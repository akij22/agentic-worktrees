import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import type { SkillDetailDto } from "../../../../shared/skills/schemas";
import { Button } from "../../../components/ui/button";
export function SkillDetail({skill,onRemove}:{skill:SkillDetailDto;onRemove():void}){return <article className="h-full overflow-auto p-6"><div className="flex items-start justify-between gap-4"><div><span className="text-[10px] font-semibold uppercase tracking-wider text-primary">Skill</span><h2 className="mt-1 text-xl font-bold">{skill.name}</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">{skill.description}</p></div><Button variant="outline" onClick={onRemove}>Remove</Button></div><dl className="mt-6 grid gap-4 border-y border-border py-4 text-xs sm:grid-cols-3"><div><dt className="text-muted-foreground">Version</dt><dd className="mt-1 font-mono">{skill.version}</dd></div><div><dt className="text-muted-foreground">Source</dt><dd className="mt-1 capitalize">{skill.source}</dd></div><div><dt className="text-muted-foreground">Automatic invocation</dt><dd className="mt-1">{skill.automaticInvocation?"Enabled":"Disabled"}</dd></div></dl><section className="mt-6"><h3 className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Instruction preview</h3><div className="prose prose-invert mt-3 max-w-none rounded-md border border-border bg-muted/30 p-4 text-sm leading-6"><ReactMarkdown remarkPlugins={[remarkGfm]} components={{h1:({children})=><h4 className="mb-3 text-base font-semibold">{children}</h4>,h2:({children})=><h4 className="mb-3 text-base font-semibold">{children}</h4>,h3:({children})=><h5 className="mb-2 text-sm font-semibold">{children}</h5>,p:({children})=><p className="mb-3 last:mb-0">{children}</p>,ul:({children})=><ul className="mb-3 list-disc space-y-1 pl-5 last:mb-0">{children}</ul>,ol:({children})=><ol className="mb-3 list-decimal space-y-1 pl-5 last:mb-0">{children}</ol>,a:({children,href})=><a className="text-primary underline underline-offset-2" href={href}>{children}</a>,code:({children})=><code className="rounded bg-muted px-1 py-0.5 font-mono text-xs">{children}</code>}}>{skill.instructionPreview}</ReactMarkdown></div></section></article>}
