// @vitest-environment jsdom
import {fireEvent,render,screen} from "@testing-library/react";import {expect,it,vi} from "vitest";import {SkillInvocationChip} from "./SkillInvocationChip";it("renders and removes one selected skill",()=>{const remove=vi.fn();render(<SkillInvocationChip skill={{id:"review",name:"Security Review",description:"Review",version:"1",source:"local",compatibility:{codex:"supported",opencode:"supported"},installationState:"installed",automaticInvocation:true}} onRemove={remove}/>);fireEvent.click(screen.getByRole("button",{name:"Remove Security Review skill"}));expect(remove).toHaveBeenCalledOnce();});
