// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from "@testing-library/react";import {afterEach,describe,expect,it,vi} from "vitest";import {MemoryRouter} from "react-router-dom";const install=vi.fn(),select=vi.fn();const capability={id:"web",name:"Web Search",description:"Search",version:"1",category:"search",source:"bundled",compatibility:{codex:"supported",opencode:"supported"},state:"ready"};const skill={id:"review",name:"Review",description:"Review code",version:"1",source:"local",compatibility:{codex:"supported",opencode:"supported"},installationState:"installed",automaticInvocation:true};vi.mock("../features/marketplace/hooks/useMarketplace",()=>({useMarketplace:()=>({items:[{kind:"capability",capability},{kind:"skill",skill}],loading:false,install,select,remove:vi.fn()})}));import {Marketplace} from "./Marketplace";afterEach(cleanup);describe("Marketplace",()=>{it("shows distinct badges and filters",()=>{render(<MemoryRouter><Marketplace/></MemoryRouter>);expect(screen.getByText("capability")).toBeTruthy();expect(screen.getByText("skill")).toBeTruthy();fireEvent.click(screen.getByRole("button",{name:"Skills"}));expect(screen.queryByText("Web Search")).toBeNull();});it("installs through the pathless API action",()=>{render(<MemoryRouter><Marketplace/></MemoryRouter>);fireEvent.click(screen.getByRole("button",{name:"Install skill"}));expect(install).toHaveBeenCalled();});it("uses the required empty-search copy",()=>{render(<MemoryRouter><Marketplace/></MemoryRouter>);fireEvent.change(screen.getByLabelText("Search marketplace"),{target:{value:"missing"}});expect(screen.getByText("No marketplace items match.")).toBeTruthy();});it("keeps the header compact before an item is selected",()=>{render(<MemoryRouter><Marketplace/></MemoryRouter>);const marketplace=screen.getByRole("region",{name:"Marketplace"});expect(marketplace.className).toContain("lg:grid-rows-[auto_minmax(0,1fr)]");});});
