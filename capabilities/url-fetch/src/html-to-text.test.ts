import { describe,expect,it } from "vitest";import { extractReadableResource } from "./html-to-text";const enc=(s:string)=>new TextEncoder().encode(s);
describe("readable extraction",()=>{it("extracts static HTML structure and safe links",()=>{const result=extractReadableResource("text/html",enc(`<title>Example documentation</title><nav>Skip</nav><h1>Install</h1><p>Read <a href="/project">Project site</a></p><ul><li>First step</li></ul><pre>npm install</pre><script>window.secret</script><style>display: none</style>`),"https://example.com/docs");expect(result.title).toBe("Example documentation");expect(result.text).toContain("# Install");expect(result.text).toContain("- First step");expect(result.text).toContain("Project site (https://example.com/project)");expect(result.text).not.toContain("window.secret");});it("normalizes text and pretty prints valid JSON",()=>{expect(extractReadableResource("text/plain",enc("a\r\n\r\n\r\nb"),"https://example.com").text).toBe("a\n\nb");expect(extractReadableResource("application/json",enc('{"a":1}'),"https://example.com").text).toBe('{\n  "a": 1\n}');expect(extractReadableResource("application/json",enc("invalid"),"https://example.com").text).toBe("invalid");});});
