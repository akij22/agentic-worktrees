import{describe,expect,it,vi}from"vitest";import{runCapabilitySmokes}from"./run.mjs";describe("smoke runner",()=>{it("selects scenarios and always closes",async()=>{const driver={launch:vi.fn(),listConfiguredAgents:vi.fn().mockResolvedValue([{kind:"codex",version:"0.150.1"},{kind:"opencode",version:"1.18.23"}]),getFirstWorktreeId:vi.fn().mockResolvedValue("w"),readProcessLogs:vi.fn(()=>""),close:vi.fn()};const one={id:"web-search",run:vi.fn().mockResolvedValue([1])},two={id:"url-fetch",run:vi.fn().mockResolvedValue([2])};await expect(runCapabilitySmokes(driver as never,[one,two] as never,{selectedScenarioIds:["url-fetch"]})).resolves.toEqual([2]);expect(one.run).not.toHaveBeenCalled();expect(driver.close).toHaveBeenCalledOnce();});it("enforces agent floors",async()=>{const driver={launch:vi.fn(),listConfiguredAgents:vi.fn().mockResolvedValue([]),close:vi.fn()};await expect(runCapabilitySmokes(driver as never,[])).rejects.toThrow("codex");expect(driver.close).toHaveBeenCalled();});});
