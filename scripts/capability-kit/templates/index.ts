import type { CapabilityNames } from "../types";
export function renderCapabilityPackage(n: CapabilityNames): ReadonlyMap<string, string> {
  const pkg = JSON.stringify({ name: n.packageName, version: "0.1.0", private: true, type: "module", exports: "./src/index.ts", dependencies: { "@agentic-worktrees/capability-sdk": "0.1.0" } }, null, 2) + "\n";
  const manifest = `import type { CapabilityManifest } from "@agentic-worktrees/capability-sdk";\nexport const ${n.manifestSymbol}: CapabilityManifest = { id: "${n.capabilityId}", name: "${n.visibleName}", version: "0.1.0", sdkVersion: "^0.1.0", description: "Generated capability scaffold.", category: "utility", author: { name: "Agentic Worktrees" }, license: "MIT", compatibility: { codex: "unsupported", opencode: "unsupported" }, permissions: { network: [], secrets: [] }, settings: {} };\n`;
  const index = `import { CapabilityError, defineCapability, defineTool, validateCapabilityDefinition } from "@agentic-worktrees/capability-sdk";\nimport { ${n.manifestSymbol} } from "./manifest";\nexport { ${n.manifestSymbol} } from "./manifest";\nexport function create${n.visibleName.replaceAll(" ", "")}Capability() { return defineCapability({ manifest: ${n.manifestSymbol}, tools: [defineTool({ name: "${n.toolName}", description: "Generated scaffold", inputSchema: { type: "object", properties: {}, additionalProperties: false }, async execute(_input, context) { if (context.signal.aborted) throw new CapabilityError("cancelled", "Capability execution was cancelled."); return { content: [{ type: "text", text: "Generated capability scaffold. Implement this reviewed tool before enabling agent compatibility." }] }; } })] }); }\nconst capability = create${n.visibleName.replaceAll(" ", "")}Capability();\nvalidateCapabilityDefinition(capability);\nexport default capability;\n`;
  const test = `import { describe, expect, it } from "vitest";\nimport capability from "./index";\nimport { validateCapabilityDefinition } from "@agentic-worktrees/capability-sdk";\ndescribe("${n.visibleName}", () => { it("defines ${n.toolName}", () => { expect(validateCapabilityDefinition(capability)).toBe(capability); expect(capability.tools.map((tool) => tool.name)).toEqual(["${n.toolName}"]); }); });\n`;
  return new Map([["package.json", pkg], ["src/manifest.ts", manifest], ["src/index.ts", index], ["src/index.test.ts", test]]);
}
