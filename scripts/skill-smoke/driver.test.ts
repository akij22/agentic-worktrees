import {describe,expect,it,vi} from "vitest";const mocks=vi.hoisted(()=>({close:vi.fn()}));vi.mock("playwright",()=>({_electron:{launch:vi.fn(async()=>({firstWindow:async()=>({evaluate:vi.fn(async()=>({installations:[]}))}),evaluate:async()=>({id:"deterministic-review",version:"1"}),close:mocks.close}))}}));import {runPackagedSkillScenarios,versionAtLeast} from "./driver.mjs";describe("skill smoke driver",()=>{it("gates provider versions deterministically",()=>{expect(versionAtLeast("2.0.0","1.9.9")).toBe(true);expect(versionAtLeast("1.2.0","1.3.0")).toBe(false);});it("closes only its owned app when provider configuration is missing",async()=>{await expect(runPackagedSkillScenarios({executable:process.execPath,provider:"codex"})).rejects.toThrow(/not configured/);expect(mocks.close).toHaveBeenCalledOnce();});});
